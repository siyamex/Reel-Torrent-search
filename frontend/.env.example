# Base URL for the backend API. The dev server proxies /api to this target
# (see vite.config.ts), so this is typically only needed for production
# builds served separately from the backend.
VITE_API_BASE_URL=/api
